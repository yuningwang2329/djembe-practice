# 从这里开始

当前版本已经可以在 Mac 浏览器和局域网 iPad 上预览，正式离线安装需要一个 HTTPS 地址。

本地启动：

```bash
cd /Users/wangyuning/Documents/Codex/2026-08-24/n/work/djembe-practice
npm install --cache .npm-cache
caffeinate -d -i -m npm run dev
```

然后让 iPad 与 Mac 使用同一 Wi-Fi，在 iPad Safari 打开终端显示的 `Network` 地址。

现在不需要操作 GitHub。你先验收“暖身律动”的谱面、声音、速度和循环；确认后再创建空的私有仓库，我再帮你接入并准备国内 HTTPS 部署。

更完整的运行、iPad 安装、测试和新曲目说明见 `README.md`。
