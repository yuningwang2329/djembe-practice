/**
 * 逐曲速度校正表：谱面标称 BPM → 从录音鼓轨实测的 BPM。
 *
 * 为什么需要：谱面标称速度若与录音实际速度不符，误差会整首累积——开头还对得上，
 * 越往后越偏。1 BPM 的差在 5 分钟的歌里就能累积成 3 秒以上。
 *
 * 测量方法：Demucs 分出鼓轨 → 起音包络网格搜索（先定相位、再扫速度、再细扫），
 * 用"拍点上的包络均值"作为得分。脚本：tools/sync-fix/report.py 的 build_beat_map()。
 *
 * 采纳规则（宁可不动，也不猜）：只有当实测峰值得分达到相邻候选的 1.5 倍以上时才采纳。
 * 未达标的曲目宁可保留原值并标记待人工复核。
 */
export const tempoCorrections: Record<string, number> = {
  /**
   * 谱面 98.5 → 实测 99.546。
   * 三重独立验证：鼓轨 99.546 / 人声轨 99.384 / 混音 99.398。
   * 细扫得分 0.1708 对 0.0981。316 秒里原本累积漂移 3.3 秒。
   */
  shuishou: 99.46,
  tongnian: 116.33,
  "zhan-zai-cao-yuan-wang-bei-jing": 102.8,
  lasa: 100.0,

  /**
   * 谱面 105.263 → 实测 105.987。
   * 细扫得分 0.5450，相邻候选仅 ~0.09（6 倍差距），峰又尖又唯一。
   * 246 秒里原本累积漂移 1.7 秒。
   */
  "huo-hong-de-sa-ri-lang": 105.987,

  /**
   * 谱面 67.84 → 实测 67.990。
   * 细扫得分 0.4668，相邻候选仅 ~0.077（6 倍差距）。
   * 231 秒里原本累积漂移 0.5 秒。
   */
  "ping-guo-xiang": 67.99,

  // 以下曲目实测与标称有差异，但证据不足，暂不采纳，留待人工复核：
  //   da-yu-zhou-shen《大鱼》标称 70.630，鼓轨 69.916（得分 0.0857 对 0.0651）
  //     —— 钢琴抒情曲，鼓能量仅 -11dB，峰不够尖，可能是锁定到了伴奏的其它周期性成分。
};
