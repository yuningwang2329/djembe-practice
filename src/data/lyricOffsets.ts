/**
 * 逐曲歌词时间微调（毫秒，加到词块高亮时刻上）。
 *
 * 由来：谱面结构校正只能按"整小节"增删，而真实偏差往往不是小节的整数倍，
 * 于是校正后必然留下不到一小节的残差。例如《北京》原谱比录音晚 1179ms，
 * 删掉一整小节（2334ms）后又反过来早了 1155ms——实测残差 +1062ms 与之吻合。
 *
 * 取值方法：把每个乐句的落点与歌词时间轴相减，取**中位数**。按中位数平移，
 * 在任意分布下都能让平均绝对误差最小，所以这一项只会改善、不会变差。
 * 只对中位偏差 ≥ 250ms 的曲目启用；更小的偏差留在噪声里，不值得动。
 *
 * 只影响歌词高亮的时刻，不影响鼓点、播放头或实际播放——实测鼓点与录音是对齐的。
 *
 * 生成/复核脚本：tools/sync-fix/plan_structure.py 配合 place_check.py
 */
export const lyricOffsets: Record<string, number> = {
  // 原谱比录音晚 699ms（21 句标准差 288ms）
  "qiao-bian-gu-niang": -700,
  // 原谱比录音晚 347ms（42 句标准差 620ms）
  tongnian: -350,
  // 原谱比录音早 265ms（24 句标准差 575ms）
  "da-yu-zhou-shen": 270,
  // 原谱比录音晚 912ms（21 句标准差 715ms）
  "ping-guo-xiang": -910,
  // 结构校正过度 1155ms 的残留：20 句标准差仅 61ms，几乎完全一致
  "zhan-zai-cao-yuan-wang-bei-jing": 1060,
};
