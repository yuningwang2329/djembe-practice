/**
 * 逐曲歌词时间微调（毫秒），**仅用于按拍位排布的回退路径**。
 *
 * 有逐字演唱时刻（lyricCharTimes）时不会用到本表——那些是 CTC 强制对齐测出的
 * 真实时刻，再叠加微调就是双重补偿。本表只在缺逐字数据、回退到"按拍位均匀
 * 排布"时才生效。
 *
 * 由来：整小节增删只能按小节的整数倍修正，真实偏差往往不是整数倍，于是校正后
 * 必然留下不到一小节的残差。
 *
 * 取值方法：把每个乐句的落点与歌词时间轴相减取中位数——按中位数平移在任意分布
 * 下都让平均绝对误差最小，所以只会改善不会变差。只收录中位偏差 ≥250ms 的曲目。
 * 复核脚本：tools/sync-fix/place_check.py
 *
 * 基准说明：桥边姑娘必须拿**原始 LRC** 做基准。它自带的时间轴里存的是那套已知
 * 错误的精修值（第二段晚 6~10 秒），拿它当基准会把正确的谱面判成错的。
 */
export const lyricOffsets: Record<string, number> = {
  // 原谱比录音晚 699ms（21 句，0 句排错超一小节）
  "qiao-bian-gu-niang": -700,
  // 原谱比录音晚 335ms
  tongnian: -350,
  // 原谱比录音早 272ms
  "da-yu-zhou-shen": 270,
  // 原谱比录音早 439ms
  shuishou: 440,
  // 原谱比录音晚 400ms
  lasa: -400,
  // 原谱比录音晚 570ms（速度校正后由 -910 更新而来）
  "ping-guo-xiang": -570,
  // 原谱比录音早 640ms
  "huo-hong-de-sa-ri-lang": 640,
  // 原谱比录音早 1066ms
  "zhan-zai-cao-yuan-wang-bei-jing": 1060,
  // 鼓楼 -135ms、惆怅客 -45ms 低于门槛，不动
};
